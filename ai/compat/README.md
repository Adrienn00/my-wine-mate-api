Compatibility wrappers kept temporarily after the AI folder split.

These files only forward execution to the real implementations under:
- `ai/xgboost/`

They are grouped here so the `ai/` root stays cleaner while we decide
whether to keep or remove them entirely later.
